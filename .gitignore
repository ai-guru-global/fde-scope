# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
.venv-build*/
venv/
env/
ENV/

# Testing & coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Tooling
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json

# Local agent-tooling state (hook state, plugin roots, run history)
.mimosa/
.v2c/
.video_agent/
.qoder/

# uv — uv.lock is committed on purpose (remediation-plan B2): CI installs
# through `uv sync --locked`, so an upstream release can't silently change
# what gets tested.

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Project artifacts (FDE run outputs)
outputs/
samples/preview.json
reports/
corpus_store/
.fde_scope/
.probe/
examples/supply_chain_ontology/out/

# Locally vendored third-party deps (not project code; keeps `ruff .` aligned with CI)
.pydeps/

# macOS app bundle build intermediates (appbuild/*.spec, launcher.py, icns are tracked)
appbuild/FDEScope.iconset/
appbuild/build/
appbuild/dist/
appbuild/build_info.json

# Locally generated design assets (exported to appbuild/*.icns; source art stays local)
vibe_images/

# Local env files (never commit; credentials live in env vars only — AGENTS.md #3)
.env
*.env
!.env.example
